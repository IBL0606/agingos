# backend/config.py

# Hvor langt tilbake scheduler/persist typisk evaluerer (minutter)
DEFAULT_LOOKBACK_MINUTES = 60

# Hvor lenge et avvik kan stå uten å bli sett før det lukkes (minutter)
DEFAULT_EXPIRE_AFTER_MINUTES = 60